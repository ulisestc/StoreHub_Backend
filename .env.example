# Django Secret Key and Debug
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=False

# Allowed Hosts (comma-separated without spaces or in standard list format)
ALLOWED_HOSTS=localhost,127.0.0.1,storehub-backend-xhr1.onrender.com

# Database URL (Clever Cloud / Production MySQL format: mysql://user:password@host:port/dbname)
DATABASE_URL=mysql://user:password@host:3306/dbname

# Alternatively, individual Database credentials (if DATABASE_URL is not set)
DB_NAME=storehub
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
